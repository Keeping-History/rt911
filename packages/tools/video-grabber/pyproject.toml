[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "video-grabber"
version = "0.1.0"
description = "IA broadcast archive → HLS pipeline for 9/11 TV recordings"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.13",
    "sqlalchemy>=2.0",
    "psycopg2-binary>=2.9",
"httpx>=0.27",
    "boto3>=1.36",
    "tenacity>=8.2",
    "prefect>=3.0",
    # fastapi<0.137 was required for Prefect 3.7.4 (PrefectRouter compat bug).
    # Prefect 3.7.6 fixed it; <0.139 verified green via dependabot PR #132.
    "fastapi<0.139",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "moto[s3]>=5.0",
    "respx>=0.21",
    "ruff>=0.4",
    "black>=24.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.black]
line-length = 100
target-version = ["py312"]
