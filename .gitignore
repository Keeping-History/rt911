.worktrees/
node_modules/
dist/
.DS_Store
.env
# Per-developer dev-server overrides (Vite loads it in dev mode only). Kept
# untracked because Playwright's webServer runs `vite -d`, so a committed copy
# would silently change the required CI E2E run too.
.env.development
tsconfig.tsbuildinfo
*.tsbuildinfo

# Large generated data files
packages/backend/entries_media.json
packages/backend/entries_news.json
/new_york_in_the_90s.glb
.claude/
/packages/frontend/.vite
/stories/archive
/docs/superpowers
usenet/**
/.github/instructions

# local secret/credential files (never commit)
/bind.txt
/login-info.txt
*.secret
.history
.pnpm-store
.remember
# mdkb's local code/doc index — regenerable sqlite, machine-specific
.mdkb/
plans/archive
packages/frontend/.husky/_/post-commit
**/.husky/_/post-commit
/packages/frontend/playwright-report/
/packages/frontend/test-results/
.playwright-mcp
.vscode
.ruff_cache
.superpowers
.pytest_cache
__pycache__/
*.pkl